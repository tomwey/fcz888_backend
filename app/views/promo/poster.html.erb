<% if @poster.blank? %>
营销注册页不存在
<% else %>
<div class="login-wrapper" style="background: <%= @poster.bg_color %>">
  <div class="top-sec">
    <%= image_tag @poster.top_img, class: 'top-img' %>
    <% if @poster.loan_product %>
    <!-- <div class="product-info">
      <%= image_tag @poster.loan_product.logo.url(:large), class: 'logo' %>
      <h2 class="name"><%= @poster.loan_product.name %></h2>
    </div> -->
    <% end %>
  </div>
  <div class="login-sec">
    <div class="login-form">
      <div class="input-box">
        <input type="tel" placeholder="请输入手机号码" id="mobile">
      </div>
      <div class="input-box">
        <input type="tel" placeholder="验证码" class="code">
        <span class="code-btn" style="color: <%= @poster.bg_color %>">获取验证码</span>
      </div>
      <div class="submit-box" style="background: <%= @poster.bg_color %>"><%= @poster.btn_text.blank? ? '立即借款' : @poster.btn_text %></div>
    </div>
    
  </div>
</div>
<% end %>